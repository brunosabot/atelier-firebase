import React from "react";

import styles from "./CardDescription.module.css";

const CardDescription = props => <div className={styles.CardDescription} {...props} />;

export default CardDescription;
